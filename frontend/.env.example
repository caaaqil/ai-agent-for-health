# ===== Frontend environment =====
# URL of the deployed backend API (note the /api suffix).
# Local dev leaves this unset and falls back to http://localhost:5000/api
VITE_API_URL=https://your-backend.onrender.com/api
